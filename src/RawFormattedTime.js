import React from 'react';
import RawFormattedBase from './RawFormattedBase';

const RawFormattedTime = props =>
  <RawFormattedBase formatter="formatTime" {...props} />;

export default RawFormattedTime;
