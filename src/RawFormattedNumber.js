import React from 'react';
import RawFormattedBase from './RawFormattedBase';

const RawFormattedNumber = props =>
  <RawFormattedBase formatter="formatNumber" {...props} />;

export default RawFormattedNumber;
