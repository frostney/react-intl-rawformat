import React from 'react';
import RawFormattedBase from './RawFormattedBase';

const RawFormattedDate = props =>
  <RawFormattedBase formatter="formatDate" {...props} />;

export default RawFormattedDate;
